logging:
  level:
    web: DEBUG
    org.springframework.data.r2dbc: DEBUG

spring:
  application.name: bootiful-web
  #devtools
  devtools:
    add-properties: true
  cloud:
    nacos:
      config:
        server-addr: alexbob.net:8848
        file-extension: yaml
  session:
    store-type: redis

