apply plugin: 'org.springframework.boot'

bootBuildImage {
    imageName = "alexbob/${project.name}"
}

dependencies {

    // 微信公众号API包
    implementation "com.github.binarywang:weixin-java-mp:${weixinVersion}"

    developmentOnly "org.springframework.boot:spring-boot-devtools"
}

